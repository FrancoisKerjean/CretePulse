# Near Me Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Page hub géolocalisée `/near-me` (plages, restos, sites, bus, météo les plus proches, géoloc 100% client-side) + tri par distance dans /explore, /beaches/today, /buses + redirects QR `/go/*`.

**Architecture:** Une lib pure `geo.ts` (haversine + tri générique) partagée client/serveur, un hook client `useGeoPosition` (activation sur clic, sessionStorage, fallback manuel via SLUG_COORDS), une page server qui précharge cb_places/food/swim-today/bus et délègue à une île client. Spec : `docs/superpowers/specs/2026-06-12-near-me-design.md`.

**Tech Stack:** Next.js 16 App Router, React 19, Tailwind v4, MapLibre GL, next-intl (UI inline 4 langues), node type-stripping pour les checks (`scripts/check-*.mjs`).

**Conventions repo (NON négociables):** git author kerjeanfrancois29 ; déploiement = push `master` PUIS `master:main` ; toute donnée chiffrée affichée → classe `font-data` ; cartes → `card-base` ; vignettes → `CardThumb` ; icônes visibles → langage maison `src/components/icons.tsx` (lucide réservé chevrons/flèches/search) ; couleurs hors tokens interdites.

---

### Task 1: Lib pure `src/lib/geo.ts`

**Files:**
- Create: `src/lib/geo.ts`
- Create: `scripts/check-geo.mjs`
- Modify: `src/lib/taxi-fare.ts` (réutiliser haversine partagé)

- [ ] **Step 1: Écrire `src/lib/geo.ts`**

```typescript
// Géo pure, zéro I/O : haversine + tri par distance générique.
// Importable client (useGeoPosition, NearMeClient), serveur et node (check-geo.mjs).
// Spec : docs/superpowers/specs/2026-06-12-near-me-design.md

export type GeoPos = { lat: number; lon: number };

export function haversineKm(a: [number, number], b: [number, number]): number {
  const [lat1, lng1] = a.map((d) => (d * Math.PI) / 180);
  const [lat2, lng2] = b.map((d) => (d * Math.PI) / 180);
  const h =
    Math.sin((lat2 - lat1) / 2) ** 2 +
    Math.cos(lat1) * Math.cos(lat2) * Math.sin((lng2 - lng1) / 2) ** 2;
  return 2 * 6371 * Math.asin(Math.sqrt(h));
}

/** Trie items par distance croissante à pos. Les items sans coords sont exclus. */
export function nearestBy<T>(
  items: T[],
  getCoords: (t: T) => [number, number] | null,
  pos: GeoPos,
  limit = 6,
): Array<T & { km: number }> {
  const out: Array<T & { km: number }> = [];
  for (const it of items) {
    const c = getCoords(it);
    if (!c) continue;
    out.push({ ...it, km: haversineKm(c, [pos.lat, pos.lon]) });
  }
  out.sort((a, b) => a.km - b.km);
  return out.slice(0, limit);
}

// Centroïde approx. de la Crète + rayon "tu es sur l'île"
export const CRETE_CENTER: [number, number] = [35.24, 24.9];
export const ON_CRETE_RADIUS_KM = 150;

export function isOnCrete(pos: GeoPos): boolean {
  return haversineKm(CRETE_CENTER, [pos.lat, pos.lon]) <= ON_CRETE_RADIUS_KM;
}
```

- [ ] **Step 2: DRY — `taxi-fare.ts` importe `haversineKm` depuis `geo.ts`** : supprimer la fonction locale `haversineKm` de `src/lib/taxi-fare.ts:102-109` et ajouter `import { haversineKm } from "./geo";` en tête. Aucune autre ligne ne change.

- [ ] **Step 3: Écrire `scripts/check-geo.mjs`** (même pattern node type-stripping que `scripts/check-bus-journey.mjs` — l'ouvrir pour copier l'en-tête d'import) :

```javascript
// node --experimental-strip-types scripts/check-geo.mjs
import { haversineKm, nearestBy, isOnCrete } from "../src/lib/geo.ts";

let fail = 0;
const ok = (name, cond) => { console.log(cond ? `ok - ${name}` : `FAIL - ${name}`); if (!cond) fail++; };

// Heraklion -> Chania vol d'oiseau ~120-145 km
const d = haversineKm([35.3387, 25.1442], [35.5138, 24.018]);
ok("haversine HER->CHQ plausible", d > 95 && d < 145);
ok("haversine zero", haversineKm([35, 25], [35, 25]) === 0);

const items = [
  { name: "a", c: [35.34, 25.14] }, { name: "b", c: [35.51, 24.02] },
  { name: "noCoords", c: null }, { name: "c", c: [35.2, 26.1] },
];
const res = nearestBy(items, (i) => i.c, { lat: 35.33, lon: 25.13 }, 2);
ok("nearestBy sort + limit", res.length === 2 && res[0].name === "a");
ok("nearestBy excludes null coords", !res.some((r) => r.name === "noCoords"));
ok("nearestBy km attached", typeof res[0].km === "number" && res[0].km < 5);

ok("isOnCrete Makrigialos", isOnCrete({ lat: 35.039, lon: 25.973 }));
ok("isOnCrete Paris false", !isOnCrete({ lat: 48.85, lon: 2.35 }));

process.exit(fail ? 1 : 0);
```

- [ ] **Step 4: Vérifier** — `node --experimental-strip-types scripts/check-geo.mjs` → tout `ok`, exit 0. Puis `npx tsc --noEmit` → 0 erreur (vérifie l'import taxi-fare).

- [ ] **Step 5: Commit** — `git add src/lib/geo.ts src/lib/taxi-fare.ts scripts/check-geo.mjs && git commit -m "feat(geo): pure haversine/nearestBy lib, shared with taxi-fare"`

---

### Task 2: Hook `useGeoPosition` + sélecteur manuel

**Files:**
- Create: `src/components/geo/useGeoPosition.ts`
- Create: `src/components/geo/PlacePicker.tsx`

- [ ] **Step 1: Écrire `src/components/geo/useGeoPosition.ts`**

```typescript
"use client";

import { useCallback, useEffect, useState } from "react";
import type { GeoPos } from "@/lib/geo";
import { SLUG_COORDS } from "@/lib/taxi-fare";

export type GeoStatus = "idle" | "prompting" | "granted" | "manual" | "denied" | "unavailable";
const KEY = "cd-geo";

/**
 * Position utilisateur, 100% client-side (ne quitte jamais le navigateur).
 * - Activation UNIQUEMENT via requestGeo() (clic), jamais au mount.
 * - setManual(slug) = fallback sélecteur (coords SLUG_COORDS).
 * - initialSlug (ex: ?from=chania-airport) initialise en "manual" sans prompt.
 * - Persistance sessionStorage pour survivre à la navigation.
 */
export function useGeoPosition(initialSlug?: string | null) {
  const [status, setStatus] = useState<GeoStatus>("idle");
  const [pos, setPos] = useState<GeoPos | null>(null);

  useEffect(() => {
    try {
      const saved = sessionStorage.getItem(KEY);
      if (saved) {
        const p = JSON.parse(saved) as GeoPos & { status: GeoStatus };
        setPos({ lat: p.lat, lon: p.lon });
        setStatus(p.status === "granted" ? "granted" : "manual");
        return;
      }
    } catch { /* sessionStorage indisponible : on reste idle */ }
    if (initialSlug && SLUG_COORDS[initialSlug]) {
      const [lat, lon] = SLUG_COORDS[initialSlug];
      setPos({ lat, lon });
      setStatus("manual");
    }
  }, [initialSlug]);

  const save = (p: GeoPos, s: GeoStatus) => {
    try { sessionStorage.setItem(KEY, JSON.stringify({ ...p, status: s })); } catch { /* noop */ }
  };

  const requestGeo = useCallback(() => {
    if (typeof navigator === "undefined" || !navigator.geolocation) { setStatus("unavailable"); return; }
    setStatus("prompting");
    navigator.geolocation.getCurrentPosition(
      ({ coords }) => {
        const p = { lat: coords.latitude, lon: coords.longitude };
        setPos(p); setStatus("granted"); save(p, "granted");
      },
      () => setStatus("denied"),
      { enableHighAccuracy: false, timeout: 10_000, maximumAge: 300_000 },
    );
  }, []);

  const setManual = useCallback((slug: string) => {
    const c = SLUG_COORDS[slug];
    if (!c) return;
    const p = { lat: c[0], lon: c[1] };
    setPos(p); setStatus("manual"); save(p, "manual");
  }, []);

  return { status, pos, requestGeo, setManual };
}
```

- [ ] **Step 2: Écrire `src/components/geo/PlacePicker.tsx`** — client component : `<select>` stylé (classes du design system, voir les selects de `src/app/[locale]/buses/JourneyPlanner.tsx` pour les classes exactes à reprendre) listant `Object.keys(SLUG_COORDS).sort()` avec labels Title Case (`slug.split("-").map(capitalize).join(" ")`), props `{ value: string | null; onChange: (slug: string) => void; label: string }`.

- [ ] **Step 3: Vérifier** — `npx tsc --noEmit` → 0 erreur.

- [ ] **Step 4: Commit** — `git commit -m "feat(geo): useGeoPosition hook (client-only, session-persisted) + PlacePicker fallback"`

---

### Task 3: Page `/[locale]/near-me`

**Files:**
- Create: `src/app/[locale]/near-me/page.tsx`
- Create: `src/components/near-me/NearMeClient.tsx`
- Modify: `src/app/sitemap.xml/route.ts:35-47` (STATIC_PAGES + "/near-me")

- [ ] **Step 1: `page.tsx` server component.** Suivre le pattern exact de `src/app/[locale]/beaches/today/page.tsx` (metadata + setRequestLocale + ISR). Spécificités :
  - `export const revalidate = 1800;`
  - Données en parallèle :
    ```typescript
    const [places, food, swim] = await Promise.all([
      getAllCbPlaces().catch(() => []),
      getAllFoodPlaces().catch(() => []),
      buildSwimToday().catch(() => null),
    ]);
    const routes = await getBusRoutes().catch(() => []); // même helper que src/app/[locale]/buses/page.tsx — ouvrir ce fichier et réutiliser sa source de routes à l'identique
    ```
  - Sérialiser vers le client UNIQUEMENT les champs utiles (payload : cb_places → slug,name,place_type,latitude,longitude,rating,photos[0] ; food → slug,name,type,cuisine,price_range,latitude,longitude,image_url ; swim.scored → top 30 par score avec beach.name/slug/latitude/longitude/imageUrl/score/rating/windCardinal/windSpeed ; swim.cities entier ; routes → place,coords via SLUG_COORDS,departures pour aujourd'hui).
  - META 4 langues (en/fr/de/el), title EN : `"Near me in Crete — beaches, food, sights & buses around you"`, desc EN : `"See what's around you right now in Crete: the calmest beach today, nearby tavernas, sights worth the detour and your next bus. Your location never leaves your browser."` (traduire fidèlement fr/de/el).
  - `alternates: buildAlternates(locale, "/near-me")`, OG type website.
  - JSON-LD FAQPage (3 Q/A par locale : nearest beach / how location works / works offline?) — pattern de `weatherPageSchema` dans `src/lib/schema.ts`.
  - Contenu statique SSR avant l'île client (1 paragraphe explicatif + liens /beaches/today, /buses, /explore) → la page n'est jamais vide pour Google.

- [ ] **Step 2: `NearMeClient.tsx`** — île client. Structure :
  - Props : `{ locale; places; food; swim; busStops }` (types sérialisés du Step 1).
  - `const { status, pos, requestGeo, setManual } = useGeoPosition(searchParams.get("from"))` — lire `?from=` via `useSearchParams()`.
  - **État idle/denied/unavailable** : carte hero `card-base` avec CiCompass, gros bouton or "Show what's around me" (`requestGeo`), ligne de confiance (icône cadenas) : EN "Your location never leaves your browser — nothing is sent to any server." + `<PlacePicker>` "or pick a place".
  - **status prompting** : spinner + texte "Locating…".
  - **pos hors Crète** (`!isOnCrete(pos)`) : message "You're not in Crete yet — pick where you'll be staying:" + PlacePicker.
  - **pos sur l'île** : rendu des 6 sections, chaque liste = `nearestBy(...)` :
    1. *Ta plage du jour* : `swim.scored` → score pondéré `s.score - Math.min(40, km * 0.8)` , max ; hero CardThumb photo + badge rating + `font-data` vent/distance.
    2. *Beaches near you* : `nearestBy(places.filter(p => p.place_type === "beach"), ...)` 6 cartes.
    3. *Eat & drink* : `nearestBy(food, ...)` 6 cartes (type, cuisine, price_range).
    4. *Worth the detour* : `nearestBy(places.filter(p => p.place_type !== "beach" && (p.rating ?? 0) >= 4), ...)` 6 cartes.
    5. *Your bus stop* : `nearestBy(busStops, ..., 1)` + 3 prochains horaires du jour (déjà calculés serveur) + CTA `/buses?from=<slug>`.
    6. *Weather near you* : `nearestBy(swim.cities, ...)` station 1 (temp, vent, mer — tout en `font-data`).
  - Liens cartes : plages/sites → `/explore` (ou page beach slug si elle existe — vérifier `src/lib/beaches.ts` getBeachBySlug usage), food → `/food/<slug>`, bus → `/buses?from=`.
  - Events : `window.plausible?.("Near Me", { props: { source, section } })` — `source` = geo|manual|qr (qr si `?utm_source=qr`), au moment de l'activation et au clic de section.
  - i18n : `const T: Record<string, …> = { en: {...}, fr: {...}, de: {...}, el: {...} }` inline, fallback EN (pattern JourneyPlanner).
  - Carte MapLibre : différé — NON incluse au MVP (YAGNI : les sections listes suffisent, la carte existe sur /explore ; l'ajouter plus tard si demandé). Le hero pointe vers /explore pour la vue carte.

- [ ] **Step 3: Sitemap** — dans `src/app/sitemap.xml/route.ts`, ajouter `"/near-me"` à STATIC_PAGES après `"/buses"`.

- [ ] **Step 4: Vérifier** — `SUPABASE_SERVICE_KEY=dummy npm run build` (workaround connu) → EXIT 0 ; `npm run dev` + curl `http://localhost:3000/en/near-me` → 200, contenu statique présent ; `http://localhost:3000/fr/near-me?from=chania-airport` → 200.

- [ ] **Step 5: Commit** — `git commit -m "feat(near-me): geolocated hub page — beaches, food, sights, bus, weather around you"`

---

### Task 4: Tri distance dans /explore

**Files:**
- Modify: `src/components/explore/ExploreView.tsx`

- [ ] **Step 1:** Ajouter dans la barre de filtres un bouton "Near me" (icône CiCompass) : `const geo = useGeoPosition()` ; au clic → `geo.requestGeo()` ; si `geo.pos` → la liste filtrée passe par `nearestBy(filtered, p => p.latitude != null ? [p.latitude, p.longitude!] : null, geo.pos, filtered.length)` au lieu de l'ordre actuel, badge `font-data` "· X km" sur chaque vignette (1 décimale sous 10 km, entier au-delà), bouton devient actif (état visuel) et re-cliquable pour désactiver (retour tri normal). `denied/unavailable` → tooltip "Location unavailable" et on n'altère rien.

- [ ] **Step 2: Vérifier** — `npx tsc --noEmit` ; dev + Playwright avec `context.setGeolocation({ latitude: 35.039, longitude: 25.973 })` (Makrigialos) sur `/en/explore` : cliquer Near me → la première vignette est un lieu du sud-est (km < 15).

- [ ] **Step 3: Commit** — `git commit -m "feat(explore): near-me distance sort with km badges"`

---

### Task 5: Plage la plus proche sur /beaches/today

**Files:**
- Create: `src/components/near-me/NearestSwimSpot.tsx`
- Modify: `src/app/[locale]/beaches/today/page.tsx` (insérer après le bloc pick du jour)

- [ ] **Step 1: `NearestSwimSpot.tsx`** — petite île client : props `{ locale; beaches: Array<{slug;name;latitude;longitude;score;rating;imageUrl}> }` (le page.tsx passe `swim.scored` filtré `rating !== "exposed"`, champs réduits). Rendu : bouton discret "Find the calm beach nearest to you" (useGeoPosition) → carte `card-base` CardThumb : nom, badge rating, `font-data` "X km away", lien. UI 4 langues inline.

- [ ] **Step 2:** L'insérer dans `beaches/today/page.tsx` après la section pick (chercher le premier bloc de rendu du pick), avec les données filtrées.

- [ ] **Step 3: Vérifier** — build OK + Playwright geoloc mockée : le bloc affiche une plage avec km cohérent.

- [ ] **Step 4: Commit** — `git commit -m "feat(beaches/today): nearest calm beach block (client-side geo)"`

---

### Task 6: "Partir d'ici" sur /buses

**Files:**
- Modify: `src/app/[locale]/buses/JourneyPlanner.tsx`

- [ ] **Step 1:** Dans JourneyPlanner, à côté du select From : bouton icône CiCompass "From here" → `useGeoPosition().requestGeo()` ; quand pos : `nearestBy(placesWithCoords, ..., 1)` sur les lieux du graphe qui ont une entrée SLUG_COORDS → `setFromPlace(nearest)`. Vérifier d'abord comment `fromPlace` mappe au nom DB (les options du select = noms DB type "Heraklion" ; mapper via `BUS_PLACE_SLUGS` inversé). Respecter l'init `?from=` existante (ne pas écraser si déjà set).

- [ ] **Step 2: Vérifier** — Playwright geoloc Makrigialos sur `/en/buses` : clic From here → From = "Makry Gyalos" (ou le lieu coords le plus proche présent dans le graphe).

- [ ] **Step 3: Commit** — `git commit -m "feat(buses): from-here geolocation shortcut in journey planner"`

---

### Task 7: Redirects QR `/go/*` + déploiement

**Files:**
- Modify: `next.config.ts`

- [ ] **Step 1:** Ajouter dans `nextConfig` (avant `headers()`) :

```typescript
async redirects() {
  const qr = (code: string, slug: string) => ({
    source: `/go/${code}`,
    destination: `/en/near-me?from=${slug}&utm_source=qr&utm_medium=print&utm_campaign=${code}`,
    permanent: false, // 307 : garder la main sur la destination tant que la campagne print n'est pas figée
  });
  return [qr("her", "heraklion"), qr("chq", "chania-airport"), qr("jsh", "sitia")];
},
```
  Note : pas de slug `heraklion-airport`/`sitia-airport` dans SLUG_COORDS → utiliser `heraklion`/`sitia` (l'aéroport HER est en ville) ; `chania-airport` existe.

- [ ] **Step 2: Vérifier** — dev : curl -I `http://localhost:3000/go/chq` → 307 vers `/en/near-me?from=chania-airport...`.

- [ ] **Step 3: Lien nav** — ajouter l'entrée Near me dans l'univers "Plan" du header (chercher le composant header où buses/airports/weather sont listés — univers Plan, Phase 13) + libellés 4 langues.

- [ ] **Step 4: Vérification finale** — `npx tsc --noEmit` 0 ; `SUPABASE_SERVICE_KEY=dummy npm run build` EXIT 0 ; Playwright rapide : /en/near-me géoloc mockée → 6 sections rendues.

- [ ] **Step 5: Commit + deploy** — `git commit -m "feat(near-me): /go/* QR redirects + nav entry"` puis `git push origin master && git push origin master:main`. Vérifier le deploy Vercel Ready puis curl prod `https://crete.direct/en/near-me` 200. ⚠️ Règle connue : pas de gros job DB VPS pendant le build Vercel ; si un deploy reste Queued >15 min, inspecter/`vercel rm` le preview du même push.
